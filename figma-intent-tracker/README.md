# Figma LinkedIn Intent Miner

This folder is the code. The full documentation — problem framing, the GTM map, the
decision log, live results, limitations, and how to run — lives in the
[repo-root README](../README.md) so it renders on the repository landing page.

Quick start (zero credentials): `pip install -r requirements.txt && MODE=demo python pipeline.py`
